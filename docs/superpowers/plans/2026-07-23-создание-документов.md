# Создание документов из контекстного меню — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Цель:** подменю «Создать» в контекстном меню списка файлов создаёт пустые документы Office и iWork копированием заготовок из бандла; список пунктов зависит от установленных приложений.

**Архитектура:** описание шаблонов и фильтрация по установленным приложениям живут в `PathwayCore` чистыми типами; граница с Launch Services спрятана за протоколом `AppLookup` и опрашивается один раз при старте. Создание файла — копирование заготовки из `Bundle.module` через `FileOperations`, дальше существующий механизм `pendingRename` открывает инлайн-редактор.

**Стек:** Swift 6, SwiftUI + AppKit, SPM, swift-testing.

**Спека:** `docs/superpowers/specs/2026-07-23-создание-документов-design.md`

## Общие ограничения

- Весь код, комментарии, тесты и сообщения коммитов — на русском.
- Тесты покрывают только `PathwayCore`; UI-тестов в проекте нет.
- `PathwayCore` не зависит от SwiftUI. AppKit допустим только ради системных сервисов (здесь — `NSWorkspace`).
- `@Suite("Русское описание")` на `struct` с английским именем `<Что>Tests`; `@Test("русская фраза")` на функциях с английскими именами.
- `swift test --filter` работает только по английским именам — фильтр по русской строке молча даёт «0 tests» и нулевой код возврата.
- Комментарии объясняют «почему», а не «что»; типичная форма — обоснование с явным «иначе …» / «а не …».
- Коммиты: русский язык, без префиксов `feat:`/`fix:`, заголовок — именная группа.
- Порядок групп в меню: базовые → Microsoft → Apple. Microsoft выше Apple.
- Bundle ID: `com.microsoft.Word`, `com.microsoft.Excel`, `com.microsoft.Powerpoint`, `com.apple.iWork.Pages`, `com.apple.iWork.Numbers`, `com.apple.iWork.Keynote`.

---

### Задача 1: Реестр шаблонов и протокол `AppLookup`

**Файлы:**
- Создать: `Sources/PathwayCore/DocumentTemplates.swift`
- Создать: `Tests/PathwayCoreTests/DocumentTemplatesTests.swift`

**Интерфейсы:**
- Отдаёт наружу: `TemplateGroup`, `DocumentTemplate` (поля `id`, `title`, `defaultName`, `fileExtension`, `group`, `requiredApp`), `AppLookup.isInstalled(bundleID:)`, `DocumentTemplates.all`, `DocumentTemplates.available(with:)`.

- [ ] **Шаг 1: Написать падающий тест**

Создать `Tests/PathwayCoreTests/DocumentTemplatesTests.swift`:

```swift
import Testing
@testable import PathwayCore

/// Подменённый поиск приложений: ничего не спрашивает у системы.
private struct StubAppLookup: AppLookup {
    let installed: Set<String>
    func isInstalled(bundleID: String) -> Bool { installed.contains(bundleID) }
}

@Suite("Список шаблонов документов")
struct DocumentTemplatesTests {
    @Test("без установленных приложений оставляет только базовые пункты")
    func basicOnly() {
        let available = DocumentTemplates.available(with: StubAppLookup(installed: []))
        #expect(available.map(\.id) == ["txt", "rtf"])
    }

    @Test("с установленным Office добавляет пункты Word, Excel и PowerPoint")
    func microsoftGroup() {
        let lookup = StubAppLookup(installed: ["com.microsoft.Word", "com.microsoft.Excel", "com.microsoft.Powerpoint"])
        let available = DocumentTemplates.available(with: lookup)
        #expect(available.map(\.id) == ["txt", "rtf", "docx", "xlsx", "pptx"])
    }

    @Test("с установленным iWork добавляет пункты Pages, Numbers и Keynote")
    func appleGroup() {
        let lookup = StubAppLookup(installed: ["com.apple.iWork.Pages", "com.apple.iWork.Numbers", "com.apple.iWork.Keynote"])
        let available = DocumentTemplates.available(with: lookup)
        #expect(available.map(\.id) == ["txt", "rtf", "pages", "numbers", "key"])
    }

    @Test("показывает обе группы сразу, а не одну вместо другой")
    func bothGroups() {
        let lookup = StubAppLookup(installed: ["com.microsoft.Word", "com.apple.iWork.Pages"])
        let available = DocumentTemplates.available(with: lookup)
        #expect(available.map(\.id) == ["txt", "rtf", "docx", "pages"])
    }

    @Test("сохраняет порядок групп: базовые, Microsoft, Apple")
    func groupOrder() {
        let groups = DocumentTemplates.all.map(\.group)
        #expect(groups == [.basic, .basic, .microsoft, .microsoft, .microsoft, .apple, .apple, .apple])
    }

    @Test("у каждого шаблона свой идентификатор")
    func uniqueIdentifiers() {
        let ids = DocumentTemplates.all.map(\.id)
        #expect(Set(ids).count == ids.count)
    }

    @Test("текстовый файл и RTF не требуют установленного приложения")
    func basicNeedNoApp() {
        let basic = DocumentTemplates.all.filter { $0.group == .basic }
        #expect(basic.allSatisfy { $0.requiredApp == nil })
    }

    @Test("имя файла шаблона совпадает с его идентификатором")
    func extensionMatchesIdentifier() {
        #expect(DocumentTemplates.all.allSatisfy { $0.id == $0.fileExtension })
    }
}
```

- [ ] **Шаг 2: Убедиться, что тест падает**

Запустить: `swift test --filter DocumentTemplatesTests`
Ожидается: ошибка компиляции — `cannot find 'DocumentTemplates' in scope`.

- [ ] **Шаг 3: Написать реализацию**

Создать `Sources/PathwayCore/DocumentTemplates.swift`:

```swift
import Foundation

/// Группа пунктов в подменю «Создать». Порядок значений задаёт порядок групп
/// в меню: сначала то, что работает всегда, затем Microsoft, затем Apple.
public enum TemplateGroup: Sendable, Equatable {
    case basic, microsoft, apple
}

/// Заготовка документа. Файл создаётся копированием, поэтому `id` служит
/// одновременно именем файла-заготовки в Resources/Templates.
public struct DocumentTemplate: Identifiable, Sendable, Equatable {
    public let id: String
    public let title: String
    public let defaultName: String
    public let fileExtension: String
    public let group: TemplateGroup
    /// Bundle ID приложения, открывающего такой файл; nil — пункт доступен всегда.
    public let requiredApp: String?
}

/// Поиск установленных приложений. Протокол, а не прямой вызов NSWorkspace:
/// иначе фильтрация списка не проверялась бы без реального Office на машине.
public protocol AppLookup: Sendable {
    func isInstalled(bundleID: String) -> Bool
}

public enum DocumentTemplates {
    public static let all: [DocumentTemplate] = [
        DocumentTemplate(
            id: "txt", title: "Текстовый документ", defaultName: "Новый документ",
            fileExtension: "txt", group: .basic, requiredApp: nil
        ),
        DocumentTemplate(
            id: "rtf", title: "Документ RTF", defaultName: "Новый документ",
            fileExtension: "rtf", group: .basic, requiredApp: nil
        ),
        DocumentTemplate(
            id: "docx", title: "Документ Word", defaultName: "Новый документ",
            fileExtension: "docx", group: .microsoft, requiredApp: "com.microsoft.Word"
        ),
        DocumentTemplate(
            id: "xlsx", title: "Книга Excel", defaultName: "Новая книга",
            fileExtension: "xlsx", group: .microsoft, requiredApp: "com.microsoft.Excel"
        ),
        DocumentTemplate(
            id: "pptx", title: "Презентация PowerPoint", defaultName: "Новая презентация",
            fileExtension: "pptx", group: .microsoft, requiredApp: "com.microsoft.Powerpoint"
        ),
        DocumentTemplate(
            id: "pages", title: "Документ Pages", defaultName: "Новый документ",
            fileExtension: "pages", group: .apple, requiredApp: "com.apple.iWork.Pages"
        ),
        DocumentTemplate(
            id: "numbers", title: "Таблица Numbers", defaultName: "Новая таблица",
            fileExtension: "numbers", group: .apple, requiredApp: "com.apple.iWork.Numbers"
        ),
        DocumentTemplate(
            id: "key", title: "Презентация Keynote", defaultName: "Новая презентация",
            fileExtension: "key", group: .apple, requiredApp: "com.apple.iWork.Keynote"
        ),
    ]

    /// Шаблоны, которые есть смысл предлагать на этой машине: пункт остаётся,
    /// если приложение-редактор установлено. Порядок `all` сохраняется — на нём
    /// держится группировка меню.
    public static func available(with apps: some AppLookup) -> [DocumentTemplate] {
        all.filter { template in
            guard let bundleID = template.requiredApp else { return true }
            return apps.isInstalled(bundleID: bundleID)
        }
    }
}
```

- [ ] **Шаг 4: Убедиться, что тесты проходят**

Запустить: `swift test --filter DocumentTemplatesTests`
Ожидается: 8 тестов пройдено.

- [ ] **Шаг 5: Коммит**

```bash
git add Sources/PathwayCore/DocumentTemplates.swift Tests/PathwayCoreTests/DocumentTemplatesTests.swift
git commit -m "Core: реестр шаблонов документов и протокол AppLookup"
```

---

### Задача 2: Опрос установленных приложений при старте

**Файлы:**
- Создать: `Sources/PathwayCore/InstalledApps.swift`
- Создать: `Tests/PathwayCoreTests/InstalledAppsTests.swift`

**Интерфейсы:**
- Использует: `AppLookup`, `DocumentTemplates.all` (задача 1).
- Отдаёт наружу: `InstalledApps`, соответствующий `AppLookup`; `InstalledApps(probe:)` для тестов и `InstalledApps()` для приложения.

- [ ] **Шаг 1: Написать падающий тест**

Создать `Tests/PathwayCoreTests/InstalledAppsTests.swift`:

```swift
import Testing
@testable import PathwayCore

/// Счётчик обращений: опрос Launch Services должен случиться один раз за сессию.
private final class CountingProbe: @unchecked Sendable {
    private(set) var queries: [String] = []
    let installed: Set<String>

    init(installed: Set<String>) { self.installed = installed }

    func probe(_ bundleID: String) -> Bool {
        queries.append(bundleID)
        return installed.contains(bundleID)
    }
}

@Suite("Опрос установленных приложений")
struct InstalledAppsTests {
    @Test("сообщает об установленном приложении")
    func reportsInstalled() {
        let probe = CountingProbe(installed: ["com.microsoft.Word"])
        let apps = InstalledApps(probe: probe.probe)
        #expect(apps.isInstalled(bundleID: "com.microsoft.Word"))
    }

    @Test("не сообщает о неустановленном приложении")
    func reportsMissing() {
        let probe = CountingProbe(installed: [])
        let apps = InstalledApps(probe: probe.probe)
        #expect(!apps.isInstalled(bundleID: "com.microsoft.Word"))
    }

    @Test("опрашивает систему один раз при создании, а не на каждый вопрос")
    func probesOnceAtStart() {
        let probe = CountingProbe(installed: ["com.microsoft.Word"])
        let apps = InstalledApps(probe: probe.probe)
        let afterInit = probe.queries.count

        for _ in 0..<10 {
            _ = apps.isInstalled(bundleID: "com.microsoft.Word")
            _ = apps.isInstalled(bundleID: "com.apple.iWork.Pages")
        }

        #expect(probe.queries.count == afterInit)
    }

    @Test("спрашивает ровно про приложения из реестра шаблонов")
    func asksAboutRegistryApps() {
        let probe = CountingProbe(installed: [])
        _ = InstalledApps(probe: probe.probe)
        let expected = Set(DocumentTemplates.all.compactMap(\.requiredApp))
        #expect(Set(probe.queries) == expected)
    }
}
```

- [ ] **Шаг 2: Убедиться, что тест падает**

Запустить: `swift test --filter InstalledAppsTests`
Ожидается: ошибка компиляции — `cannot find 'InstalledApps' in scope`.

- [ ] **Шаг 3: Написать реализацию**

Создать `Sources/PathwayCore/InstalledApps.swift`:

```swift
import AppKit
import Foundation

/// Набор приложений, установленных на машине. Опрос Launch Services делается
/// один раз, в инициализаторе: menuNeedsUpdate вызывается на каждый правый
/// клик, и шесть обращений к системе оттуда — работа на горячем пути.
///
/// Плата за это — Office, установленный при запущенном приложении, появится в
/// меню только после перезапуска. Отслеживать установку на лету через
/// NSWorkspace.didLaunchApplicationNotification не стали: сложность ради
/// события, которого почти не бывает.
public struct InstalledApps: AppLookup {
    private let found: Set<String>

    /// `probe` подменяется в тестах: настоящий NSWorkspace ответил бы про
    /// приложения конкретной машины, и проверка зависела бы от того, где её
    /// запустили.
    public init(probe: (String) -> Bool = InstalledApps.systemProbe) {
        let needed = Set(DocumentTemplates.all.compactMap(\.requiredApp))
        found = Set(needed.filter(probe))
    }

    public func isInstalled(bundleID: String) -> Bool {
        found.contains(bundleID)
    }

    public static func systemProbe(_ bundleID: String) -> Bool {
        NSWorkspace.shared.urlForApplication(withBundleIdentifier: bundleID) != nil
    }
}
```

- [ ] **Шаг 4: Убедиться, что тесты проходят**

Запустить: `swift test --filter InstalledAppsTests`
Ожидается: 4 теста пройдено.

- [ ] **Шаг 5: Коммит**

```bash
git add Sources/PathwayCore/InstalledApps.swift Tests/PathwayCoreTests/InstalledAppsTests.swift
git commit -m "Core: разовый опрос установленных приложений при старте"
```

---

### Задача 3: Заготовки документов в ресурсах

**Файлы:**
- Создать: `Sources/PathwayCore/Resources/Templates/{txt,rtf,docx,xlsx,pptx,pages,numbers,key}`
- Изменить: `Package.swift`
- Создать: `Tests/PathwayCoreTests/TemplateResourcesTests.swift`

**Интерфейсы:**
- Использует: `DocumentTemplates.all` (задача 1).
- Отдаёт наружу: `DocumentTemplates.templatesRoot` — URL папки с заготовками внутри `Bundle.module`.

**Состояние заготовок.** Пять из восьми уже лежат в `Sources/PathwayCore/Resources/Templates` и проверены:

| Файл | Происхождение | Проверка |
|---|---|---|
| `txt` | пустой файл | — |
| `rtf` | минимальный заголовок RTF | разбирается `NSAttributedString`, текста 0 |
| `docx` | собран кодом (ZIP + XML) | Word открыл, 1 абзац |
| `xlsx` | собран кодом | Excel открыл, 1 лист |
| `pptx` | собран кодом | PowerPoint открыл, 1 слайд |

Недостающие `pages`, `numbers`, `key` сохраняются вручную из соответствующих приложений: формат закрытый, внутри бинарный IWA-снапшот, сочинить его нельзя. Класть в ту же папку под именем без расширения (`pages`, а не `Документ.pages`).

**Пустой файл нулевой длины для этих шести форматов не подойдёт** — приложение сочтёт его повреждённым. Шаг 1 проверяет именно это.

- [ ] **Шаг 1: Написать падающий тест**

Создать `Tests/PathwayCoreTests/TemplateResourcesTests.swift`:

```swift
import Foundation
import Testing
@testable import PathwayCore

@Suite("Заготовки документов в бандле")
struct TemplateResourcesTests {
    @Test("для каждого шаблона в бандле лежит заготовка")
    func everyTemplateHasFile() throws {
        for template in DocumentTemplates.all {
            let url = DocumentTemplates.templatesRoot.appendingPathComponent(template.id)
            #expect(FileManager.default.fileExists(atPath: url.path), "нет заготовки \(template.id)")
        }
    }

    @Test("заготовки контейнерных форматов непустые, иначе приложение не откроет файл")
    func containerTemplatesAreNotEmpty() throws {
        // .txt намеренно нулевой длины — пустой текстовый файл это нормально.
        // Остальные форматы суть ZIP или размеченный текст: нулевой файл
        // Word и Pages считают повреждённым.
        for template in DocumentTemplates.all where template.id != "txt" {
            let url = DocumentTemplates.templatesRoot.appendingPathComponent(template.id)
            let size = try FileManager.default.attributesOfItem(atPath: url.path)[.size] as? Int ?? 0
            #expect(size > 0, "заготовка \(template.id) пуста")
        }
    }

    @Test("заготовки OOXML и iWork — ZIP-контейнеры")
    func containersAreZip() throws {
        let zipFormats = ["docx", "xlsx", "pptx", "pages", "numbers", "key"]
        for id in zipFormats {
            let url = DocumentTemplates.templatesRoot.appendingPathComponent(id)
            let handle = try FileHandle(forReadingFrom: url)
            defer { try? handle.close() }
            let magic = try handle.read(upToCount: 2)
            #expect(magic == Data([0x50, 0x4B]), "заготовка \(id) не ZIP")
        }
    }
}
```

- [ ] **Шаг 2: Убедиться, что тест падает**

Запустить: `swift test --filter TemplateResourcesTests`
Ожидается: ошибка компиляции — `type 'DocumentTemplates' has no member 'templatesRoot'`.

- [ ] **Шаг 3: Положить заготовки и подключить ресурсы**

`txt`, `rtf`, `docx`, `xlsx`, `pptx` уже на месте. Добавить три файла iWork и проверить весь набор:

```bash
for f in docx xlsx pptx pages numbers key; do
  printf '%-8s %s\n' "$f" "$(file -b Sources/PathwayCore/Resources/Templates/$f)"
done
```

Ожидается: для `docx`/`xlsx`/`pptx` — `Microsoft Word 2007+` и аналоги, для файлов iWork — `Zip archive data`.

Изменить `Package.swift` — у `PathwayCore` появляется секция ресурсов:

```swift
.target(
    name: "PathwayCore",
    resources: [.copy("Resources/Templates")]
),
```

Добавить в конец `Sources/PathwayCore/DocumentTemplates.swift`:

```swift
extension DocumentTemplates {
    /// Папка с заготовками внутри бандла. Промах означал бы, что ресурсы не
    /// подключены в Package.swift, — это ошибка сборки, а не выполнения.
    public static var templatesRoot: URL {
        guard let url = Bundle.module.url(forResource: "Templates", withExtension: nil) else {
            preconditionFailure("Заготовки документов не подключены к PathwayCore")
        }
        return url
    }
}
```

- [ ] **Шаг 4: Убедиться, что тесты проходят**

Запустить: `swift test --filter TemplateResourcesTests`
Ожидается: 3 теста пройдено.

- [ ] **Шаг 5: Коммит**

```bash
git add Package.swift Sources/PathwayCore/Resources Sources/PathwayCore/DocumentTemplates.swift Tests/PathwayCoreTests/TemplateResourcesTests.swift
git commit -m "Ресурсы: пустые заготовки документов в бандле PathwayCore"
```

---

### Задача 4: Создание файла из заготовки

**Файлы:**
- Изменить: `Sources/PathwayCore/FileOperations.swift` (добавить метод после `createFolder`, строка 27)
- Создать: `Tests/PathwayCoreTests/CreateDocumentTests.swift`

**Интерфейсы:**
- Использует: `DocumentTemplate` (задача 1), приватный `uniqueURL(in:name:extension:)` (`FileOperations.swift:106`).
- Отдаёт наружу: `FileOperations.createDocument(_:in:templatesRoot:) throws -> URL`, `FileOperationError.templateMissing`.

- [ ] **Шаг 1: Написать падающий тест**

Создать `Tests/PathwayCoreTests/CreateDocumentTests.swift`. `withTempDir` объявлен на верхнем уровне в `DirectoryLoaderTests.swift` и доступен отсюда:

```swift
import Foundation
import Testing
@testable import PathwayCore

@Suite("Создание документа из заготовки")
struct CreateDocumentTests {
    private let template = DocumentTemplate(
        id: "docx", title: "Документ Word", defaultName: "Новый документ",
        fileExtension: "docx", group: .microsoft, requiredApp: "com.microsoft.Word"
    )

    /// Кладёт в папку заготовку с заданным содержимым и возвращает её корень.
    private func makeTemplatesRoot(in dir: URL, content: Data = Data([0x50, 0x4B, 0x03, 0x04])) throws -> URL {
        let root = dir.appendingPathComponent("Templates")
        try FileManager.default.createDirectory(at: root, withIntermediateDirectories: true)
        try content.write(to: root.appendingPathComponent("docx"))
        return root
    }

    @Test("создаёт файл с именем и расширением из шаблона")
    func createsNamedFile() throws {
        try withTempDir { dir in
            let root = try makeTemplatesRoot(in: dir)
            let url = try FileOperations().createDocument(template, in: dir, templatesRoot: root)
            #expect(url.lastPathComponent == "Новый документ.docx")
            #expect(FileManager.default.fileExists(atPath: url.path))
        }
    }

    @Test("второй документ получает имя со счётчиком, а не перезаписывает первый")
    func secondDocumentGetsSuffix() throws {
        try withTempDir { dir in
            let root = try makeTemplatesRoot(in: dir)
            let operations = FileOperations()
            let first = try operations.createDocument(template, in: dir, templatesRoot: root)
            let second = try operations.createDocument(template, in: dir, templatesRoot: root)
            #expect(second.lastPathComponent == "Новый документ 2.docx")
            #expect(FileManager.default.fileExists(atPath: first.path))
        }
    }

    @Test("содержимое совпадает с заготовкой байт в байт")
    func copiesContentExactly() throws {
        try withTempDir { dir in
            let content = Data([0x50, 0x4B, 0x03, 0x04, 0x14, 0x00])
            let root = try makeTemplatesRoot(in: dir, content: content)
            let url = try FileOperations().createDocument(template, in: dir, templatesRoot: root)
            #expect(try Data(contentsOf: url) == content)
        }
    }

    @Test("без заготовки бросает ошибку, а не создаёт пустой файл")
    func missingTemplateThrows() throws {
        try withTempDir { dir in
            let root = dir.appendingPathComponent("Пусто")
            try FileManager.default.createDirectory(at: root, withIntermediateDirectories: true)
            #expect(throws: FileOperationError.templateMissing) {
                try FileOperations().createDocument(template, in: dir, templatesRoot: root)
            }
            let created = try FileManager.default.contentsOfDirectory(atPath: dir.path)
            #expect(!created.contains { $0.hasSuffix(".docx") })
        }
    }
}
```

- [ ] **Шаг 2: Убедиться, что тест падает**

Запустить: `swift test --filter CreateDocumentTests`
Ожидается: ошибка компиляции — `value of type 'FileOperations' has no member 'createDocument'`.

- [ ] **Шаг 3: Написать реализацию**

В `Sources/PathwayCore/FileOperations.swift` дополнить перечисление ошибок:

```swift
public enum FileOperationError: Error, Equatable {
    case invalidName
    case nameAlreadyExists
    case templateMissing
}
```

И добавить метод сразу после `createFolder`:

```swift
    /// Создаёт документ копированием заготовки из бандла: так файл появляется
    /// мгновенно и без запуска Word или Pages.
    ///
    /// `templatesRoot` передаётся параметром, а не берётся из Bundle.module
    /// внутри: иначе операцию нельзя было бы проверить на временной папке.
    public func createDocument(
        _ template: DocumentTemplate,
        in directory: URL,
        templatesRoot: URL
    ) throws -> URL {
        let source = templatesRoot.appendingPathComponent(template.id)
        guard fm.fileExists(atPath: source.path) else {
            throw FileOperationError.templateMissing
        }
        let target = uniqueURL(in: directory, name: template.defaultName, extension: template.fileExtension)
        try fm.copyItem(at: source, to: target)
        return target
    }
```

- [ ] **Шаг 4: Убедиться, что тесты проходят**

Запустить: `swift test --filter CreateDocumentTests`
Ожидается: 4 теста пройдено.

Затем прогнать весь набор — `describe(_:at:)` в `BrowserModel` разбирает `FileOperationError` через `switch`, и новый случай мог сделать его неполным:

Запустить: `swift test`
Ожидается: все тесты проходят. Если сборка падает на `switch must be exhaustive` в `BrowserModel.describe`, добавить туда ветку:

```swift
            case .templateMissing:
                return "Не удалось создать документ: заготовка отсутствует в приложении."
```

- [ ] **Шаг 5: Коммит**

```bash
git add Sources/PathwayCore/FileOperations.swift Sources/PathwayCore/BrowserModel.swift Tests/PathwayCoreTests/CreateDocumentTests.swift
git commit -m "Core: создание документа копированием заготовки"
```

---

### Задача 5: Обвязка в модели и состоянии

**Файлы:**
- Изменить: `Sources/PathwayCore/BrowserModel.swift` (метод рядом с `createFolder`, строка 306)
- Изменить: `Sources/PathwayCore/AppState.swift` (поле `appLookup` и параметр инициализатора)
- Создать: `Tests/PathwayCoreTests/BrowserCreateDocumentTests.swift`

**Интерфейсы:**
- Использует: `FileOperations.createDocument` (задача 4), `DocumentTemplates.templatesRoot` (задача 3), `InstalledApps` (задача 2), приватный `run { }` (`BrowserModel.swift:441`).
- Отдаёт наружу: `BrowserModel.createDocument(_:) -> URL?`, `AppState.appLookup`.

- [ ] **Шаг 1: Написать падающий тест**

Создать `Tests/PathwayCoreTests/BrowserCreateDocumentTests.swift`:

```swift
import Foundation
import Testing
@testable import PathwayCore

@MainActor
@Suite("Создание документа из модели")
struct BrowserCreateDocumentTests {
    private let template = DocumentTemplate(
        id: "txt", title: "Текстовый документ", defaultName: "Новый документ",
        fileExtension: "txt", group: .basic, requiredApp: nil
    )

    @Test("создаёт документ в текущей папке и возвращает его адрес")
    func createsInCurrentFolder() throws {
        try withTempDir { dir in
            let model = BrowserModel(path: dir)
            let url = model.createDocument(template)
            #expect(url?.lastPathComponent == "Новый документ.txt")
            #expect(FileManager.default.fileExists(atPath: url!.path))
        }
    }

    @Test("при неудаче возвращает nil и показывает ошибку, а не молчит")
    func reportsFailure() throws {
        try withTempDir { dir in
            let missing = DocumentTemplate(
                id: "нет-такого", title: "Не существует", defaultName: "Документ",
                fileExtension: "xyz", group: .basic, requiredApp: nil
            )
            let model = BrowserModel(path: dir)
            #expect(model.createDocument(missing) == nil)
            #expect(model.errorMessage != nil)
        }
    }
}
```

- [ ] **Шаг 2: Убедиться, что тест падает**

Запустить: `swift test --filter BrowserCreateDocumentTests`
Ожидается: ошибка компиляции — `value of type 'BrowserModel' has no member 'createDocument'`.

- [ ] **Шаг 3: Написать реализацию**

В `Sources/PathwayCore/BrowserModel.swift` рядом с `createFolder` добавить:

```swift
    /// Возвращает адрес созданного документа, чтобы вью открыл на нём
    /// инлайн-редактор; nil — операция не удалась, текст уже в errorMessage.
    @discardableResult
    public func createDocument(_ template: DocumentTemplate) -> URL? {
        var created: URL?
        run {
            created = try operations.createDocument(
                template, in: pane.path, templatesRoot: DocumentTemplates.templatesRoot
            )
        }
        return created
    }
```

В `Sources/PathwayCore/AppState.swift` добавить поле после `onboarding`:

```swift
    /// Приложения, найденные при запуске: от них зависит состав подменю
    /// «Создать». Опрос разовый — см. InstalledApps.
    public let appLookup: any AppLookup
```

И параметр инициализатора со значением по умолчанию, чтобы существующие вызовы не менялись:

```swift
    public init(
        path: URL = FileManager.default.homeDirectoryForCurrentUser,
        favorites: FavoritesStore = FavoritesStore(),
        terminal: TerminalLauncher = TerminalLauncher(),
        onboarding: OnboardingModel = OnboardingModel(),
        appLookup: any AppLookup = InstalledApps()
    ) {
        self.browser = BrowserModel(path: path)
        self.favorites = favorites
        self.folderActions = FolderActions(favorites: favorites, terminal: terminal)
        self.onboarding = onboarding
        self.appLookup = appLookup
    }
```

- [ ] **Шаг 4: Убедиться, что тесты проходят**

Запустить: `swift test --filter BrowserCreateDocumentTests`
Ожидается: 2 теста пройдено.

Запустить: `swift test`
Ожидается: весь набор проходит.

- [ ] **Шаг 5: Коммит**

```bash
git add Sources/PathwayCore/BrowserModel.swift Sources/PathwayCore/AppState.swift Tests/PathwayCoreTests/BrowserCreateDocumentTests.swift
git commit -m "Core: создание документа в модели и список приложений в AppState"
```

---

### Задача 6: Подменю «Создать» в контекстном меню

**Файлы:**
- Изменить: `Sources/Pathway/FileListView.swift` (`menuNeedsUpdate`, строка 514; методы координатора)

**Интерфейсы:**
- Использует: `DocumentTemplates.available(with:)` (задача 1), `AppState.appLookup` (задача 5), `BrowserModel.createDocument` (задача 5), существующие `add(to:_:_:)`, `appState.pendingRename`.

Тестов у этой задачи нет: UI-тестов в проекте нет принципиально, проверка — ручным чек-листом.

- [ ] **Шаг 1: Заменить пункт «Новая папка» на подменю**

В `Sources/Pathway/FileListView.swift`, в `menuNeedsUpdate`, заменить строку `add(to: menu, .newFolder, #selector(menuNewFolder))` на:

```swift
            addCreateSubmenu(to: menu)
```

- [ ] **Шаг 2: Добавить построение подменю**

В `Coordinator` добавить методы (рядом с прочими методами меню):

```swift
        /// Подменю «Создать»: папка плюс документы, которые есть чем открыть.
        /// Строится из DocumentTemplates, а не из CommandRegistry: реестр
        /// описывает команды с постоянным идентификатором, а список шаблонов
        /// зависит от установленных приложений и потому переменной длины.
        private func addCreateSubmenu(to menu: NSMenu) {
            let submenu = NSMenu()
            add(to: submenu, .newFolder, #selector(menuNewFolder), title: "Папка")

            let writable = !model.isReadOnlyVolume
            var lastGroup: TemplateGroup?
            for template in DocumentTemplates.available(with: appState.appLookup) {
                // Разделитель перед группой, а не после каждой: иначе на машине
                // без Office в меню осталась бы висячая черта.
                if template.group != lastGroup {
                    submenu.addItem(.separator())
                    lastGroup = template.group
                }
                let item = NSMenuItem(
                    title: template.title,
                    action: writable ? #selector(menuCreateDocument(_:)) : nil,
                    keyEquivalent: ""
                )
                item.target = self
                item.representedObject = template
                submenu.addItem(item)
            }

            let parent = NSMenuItem(title: "Создать", action: nil, keyEquivalent: "")
            parent.submenu = submenu
            menu.addItem(parent)
        }

        @objc private func menuCreateDocument(_ sender: NSMenuItem) {
            guard let template = sender.representedObject as? DocumentTemplate else { return }
            // Новый файл сразу уходит в переименование — как «Новая папка» и как
            // в проводнике: имя по умолчанию человеку почти всегда не нужно.
            appState.pendingRename = model.createDocument(template)
        }
```

- [ ] **Шаг 3: Проверить сборку**

Запустить: `swift build`
Ожидается: сборка без ошибок.

Если компилятор жалуется, что `add(to:_:_:title:)` не принимает `NSMenu`, отличный от параметра, — проверить сигнатуру существующего `add` в координаторе и при необходимости расширить её первым параметром `NSMenu` (он там уже есть: `add(to: menu, …)`).

Если `model.isReadOnlyVolume` недоступен под этим именем — свериться с `CommandRegistry`, где условие записано как `$0.browser.isReadOnlyVolume`.

- [ ] **Шаг 4: Проверить весь набор тестов**

Запустить: `swift test`
Ожидается: все тесты проходят.

- [ ] **Шаг 5: Коммит**

```bash
git add Sources/Pathway/FileListView.swift
git commit -m "UI: подменю «Создать» с документами Office и iWork"
```

---

### Задача 7: Ручная проверка и правка документации

**Файлы:**
- Изменить: `CLAUDE.md` (раздел «Архитектура» — короткий абзац о подменю)

- [ ] **Шаг 1: Собрать приложение**

Собрать бандл во временной директории, чтобы не трогать установленную копию (`./build-app.sh` закрывает запущенное приложение и перезаписывает `/Applications/Проводник.app`).

- [ ] **Шаг 2: Пройти ручной чек-лист из спеки**

Пункты 1–10 раздела «Ручной чек-лист» в `docs/superpowers/specs/2026-07-23-создание-документов-design.md`. Ключевые: документ Word открывается без жалоб на повреждение; второй документ подряд называется «Новый документ 2»; на машине без Office лишнего разделителя нет; `⇧⌘N` по-прежнему создаёт папку.

- [ ] **Шаг 3: Дополнить CLAUDE.md**

В раздел «Реестр команд» добавить абзац:

```markdown
Подменю «Создать» — намеренное исключение из реестра. Пункты документов
строятся из `DocumentTemplates.available(with:)`, а не из `CommandID`: реестр
описывает команды с постоянным идентификатором, а состав шаблонов зависит от
установленных приложений. В реестре остаётся только «Новая папка» — у неё есть
свой `⇧⌘N`. Наличие Office и iWork опрашивается один раз при старте
(`InstalledApps`), потому что `menuNeedsUpdate` выполняется на каждый правый
клик.
```

- [ ] **Шаг 4: Коммит**

```bash
git add CLAUDE.md
git commit -m "Документация: подменю «Создать» в разделе о реестре команд"
```

---

## Самопроверка плана

**Покрытие спеки.** `DocumentTemplates.swift` — задача 1; `InstalledApps.swift` — задача 2; `Resources/Templates` и `Package.swift` — задача 3; `FileOperations.createDocument` — задача 4; `BrowserModel` и `AppState` — задача 5; подменю в `FileListView` — задача 6; ручной чек-лист и документация — задача 7. Раздел спеки «Выделение имени без расширения» задачи не требует: `selectNameWithoutExtension` уже существует (`FileListView.swift:347`), в плане он проверяется пунктом чек-листа.

**Заглушек нет:** каждый шаг содержит готовый код или точную команду с ожидаемым результатом.

**Согласованность имён:** `DocumentTemplate` создаётся в задаче 1 и используется в 3–6 с теми же полями; `templatesRoot` объявлен в задаче 3 и вызван в задаче 5; `createDocument` имеет одну сигнатуру в задачах 4 и 5; `FileOperationError.templateMissing` заводится в задаче 4 и там же обрабатывается в `describe`.
